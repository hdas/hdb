#define __expr_h

#ifndef __db_h
#include "db.h"
#endif

#ifndef __token_h
#include "token.h"
#endif

/*
int initstack(DBStack * stack);
int emptystack(DBStack * stack);
*/

//DBVariable * eval(char * stl[], int st, int nd);
//DBVariable * eval_with_table(char * stl[], int st, int lt, DBTable * tbl, int nTbl, int * Result);
//DBVariable * eval_with_table(DBExpr * the_expr_to_eval, DBTable ** paTable, int nTable, int * pRetcd);
//DBVariable * eval_pf(DBStack * pftlist, int * pRetcd);
//int postfix(char * stl[], DStack * pftlist, int st, int lt, DTable ** paTable, int nTable);

//int icp(int oprtr);
//int isp(int oprtr);
//char * PerformOparation(char * op1, char * op2, int oprtr, int * Result);
//DBVariable * PerformOparation(DBVariable * vop1, DBVariable * vop2, int oprtr, int * retcd);

//DBExpr * dCreateExpr(char * stl[], int st, int lt, DBTable ** paTable, int nTable, int * retcd);
//DBExpr * dCreateExprArray(char * stl[], int qry_type, int * nCol,  DBTable * tbl, int nTbl, int * retcd);
//int dDestroyExprArray(DBExpr * expr_arr, int nCol);
//int dDestroyExpr(DBExpr * expr);



